#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

// WiFi credentials - these are used as fallback if network profiles don't specify their own
#define WIFI_SSID "YourNetworkName"
#define WIFI_PASSWORD "YourPassword"

// API Ninjas API key for quote service
// Get your free API key at: https://api-ninjas.com/
#define API_NINJAS_KEY "YOUR_API_KEY_HERE"

// Network Profiles Configuration
// Define your network profiles with static IP and DNS settings
// Each profile can have its own SSID and password
#define NETWORK_PROFILES \
  { \
    "Home", \
    IPAddress(192, 168, 1, 61), \
    IPAddress(192, 168, 1, 1), \
    IPAddress(255, 255, 255, 0), \
    IPAddress(8, 8, 8, 8), \
    IPAddress(8, 8, 4, 4), \
    "YourHomeSSID", \
    "YourHomePassword" \
  }, \
  { \
    "Work", \
    IPAddress(172, 31, 0, 250), \
    IPAddress(172, 31, 0, 1), \
    IPAddress(255, 255, 254, 0), \
    IPAddress(1, 1, 1, 1), \
    IPAddress(9, 9, 9, 9), \
    "YourWorkSSID", \
    "YourWorkPassword" \
  }

// Instructions:
// 1. Copy this file to wifi_credentials.h
// 2. Replace the placeholder values with your actual credentials
// 3. Update network profiles with your IP addresses, DNS servers, and WiFi credentials
// 4. The wifi_credentials.h file will be ignored by git

#endif
